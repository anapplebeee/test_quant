# 2026-08-27

## 后端优化 + 提交 (master 7081e60)
- 修复 .gitignore：`data/` 改为根锚定 `/data/`。原非锚定模式误忽略后端包 `quart/data/`（store.py/source_akshare.py/updater.py/universe.py/__init__.py），数据层源码此前从未入库。
- `quart/data/store.py`：`load(symbols=[...])` 原会 glob 整个日线目录（全市场增量更新退化为 O(N²)）；改为按 `_path` 直读单文件（`_load_symbols` 私有方法）。同步清理内联 `import logging` / `import datetime`。
- `quart/pipeline.py`：`run_daily` 风控校验 `validate_weights` 改用持仓真实权益（cash+市值）替代 `initial_cash`，与 `generate_orders` 基数一致。
- 测试：项目无 uv，用 `.venv/Scripts/python.exe -m pytest`；20 passed。
- 提交范围：quart/ + .gitignore（本次实际 quart/data/* 新增 + pipeline.py 修改 + .gitignore）。排除前端 pages/*、streamlit_app.py、_probe 草稿、.agents/。
- 注意：仓库存在 CRLF 行尾归一化，git status 的 modified/untracked 多为行尾噪声；判断真实改动以 `git ls-tree HEAD` / `git diff HEAD` 为准。
