# 2026-09-01 工作日志

## 收益率优化专项：会话收尾交接（当前进度 + 遗留问题）

### 最终结论
- **不落地任何参数变更**，`config/settings.yaml` 保持现状。候选"lowvol_indz + rev_weight=0.4 +
  rebalance_days=20 + regime_filter_days=120"样本内 CAGR 20.2%/Sharpe 1.10，但冻结纯 OOS
  （--start 2025-09-01）**-9.92%** 否决；WFA 数值通过（衰减比 2.17）但 2 折窗口止于 2026-05-22，
  恰好躲过 7 月科技拥挤度逆转——**WFA 窗口截断会美化结果，短历史下不能单独信**。
  预登记回退方案（regime_filter_days=20）同窗口对照亦劣于基线（-4.28% vs -3.30%）。
- 诊断成立（全市场证据）：基线真行业中性累计 +1.67%、超额年化 -10.19%；低波三件套全市场
  IC≈0（小池 0.065 不可外推）；有效信号是反转族（rev5 +0.090、rel_ind_rev20 ICIR 0.74）；
  动量族全灭（mom60 -0.095）。当前配置 rev_weight=0 + 45 天调仓 + MA20 择时把仅有的有效信号闲置了。

### 本会话交付（commit 06bebb2，12 文件 +1227/-24）
- `quart/research/factor_audit.py` 全市场审计框架 + 4 新因子（size_neg/turn20_neg/ep_ttm/bp）；
  修复 compute() None 崩溃与行业映射缺失时的静默降级。
- `quart/data/fundamental.py` baostock PIT 基本面加载器；`scripts/backfill_factor_data.py`
  多进程并行回填（独立连接 + part 文件合并 + 断点续传 + 限流退避 + 30s socket 超时）。
- `lowvol_composite` size/turnover/value 风格权重 + `run_backtest` 对应 CLI。
- 修复 3 例 rebase 遗留测试脱节（旧版串行更新器测试 vs 并发 _Throttle 新实现），
  保留安全语义（全量刷新需确认、覆盖式重拉、远端空不删本地）。全量 398 测试通过、ruff 干净。
- 报告 `reports/quant_optimization_2026-08-31.md`（gitignore 本地留存）：市场调研 12 来源 +
  诊断 + 审计 + 实验 + §5 最终结论与六项后续建议。

### 遗留问题（按优先级）
1. **数据回填停在 1107/3367（33%）**（✅ 已闭环：2026-09-01 08:29 回填至 3028 只并完成全覆盖复审计，
   结论见本文末"基本面回填收尾 + 全市场复审计"一节）：baostock IP 级限流（单会话 ~1000+ 查询后触发，登录成功但
   查询全部超时，跨零点未解除）。续跑：`.venv/Scripts/python.exe scripts/backfill_factor_data.py
   --start 2023-12-01 --workers 3`（退避自愈，完成后自动合并）。之后重跑因子审计确认
   size_neg（8% 覆盖时曾 IC +0.092）/turn20_neg/ep_ttm/bp，更新报告 §3；若通过门禁再开独立实验轮。
2. **工作树留有另一条并行工作线未入库产物**（quart/infrastructure/、api/portfolio_api.py、
   scripts/mine_factors.py、docs/adr/0001、test_engine signal_offset 改造等，测试全绿）——归属待确认。
3. **数据质量治理 `--apply`**（419 行异常/161 符号）待人工决策；未 apply 前回测含异常符号。
4. 基本面数据为静态回填快照，实盘每日信号需接入日增量更新；2019-2023 全市场 bars 回填、
   申万行业升级（akshare 接口不稳，现用统计聚类）、创业板/科创板入池均为后续项。
5. 小市值因子拥挤度崩溃风险（2024-02、2026-07 两次危机）——流动性底线 5000 万不动，
   建议后续加拥挤度监控。

### 环境教训
- baostock：阻塞 recv 无超时会静默挂死（已加 30s 超时）；会限流，长回填必须带退避与续传。
- 权限策略拦截 tasklist 与长 sleep：验证后台任务存活用"日志进度行 + 落盘账目对账"。
- 全市场数据仅 2024-2026（~2.7 年），WFA 折数有限，判据应以"因子时序稳定性 + 纯 OOS 冻结"为主。

## 基本面回填收尾 + 全市场复审计（03:30 定时任务，08:29 完成）

- 回填：孤儿 worker 已全死、静默 77 分钟后才续跑。主文件 1107 → 2034（先手工合并 part 保进度）→
  **3028 只 / 1,985,799 行 / 2023-12-01 ~ 2026-08-31**，(date,symbol) 零重复、七字段零空值，
  float_mcap 中位 63.3 亿 / p95 741.8 亿。**baostock 可达上限是 3045 只**（920 开头 322 只北交所代码
  被 `baostock_code()` 直接判 None，永不成功），真实网络失败只剩 17 只（600001/600002/600013… 本地
  bars 整齐 260 行、末日统一 2024-12-27，疑与 data_quality_scan 的 161 异常符号同源，未重试）。
  审计池本身已剔除北交所，故 93% 覆盖即接近满覆盖。
- **复审计过门禁的因子清单：`size_neg`（新增）、`rel_ind_rev20`、`rev5`** 共 3 个候选。
  `size_neg` IC +0.099 / ICIR +0.76（全表第一）/ q 0.005 / 正例率 0.76 / 覆盖 93%。
  `bp` 停在观察（IC +0.033 但 ICIR 0.15、q 0.88）；`turn20_neg` IC -0.0008、`ep_ttm` IC -0.0010 **判死**。
- **建议：只开 `size_weight` 单权重实验轮，turnover / value 权重不上。** 该轮必须带三项风险检验：
  ① size_neg 与已淘汰的 amihud20_neg 相关 **-0.82**（可能只是非流动性暴露的镜像，需正交化后重测）；
  ② early IC +0.144 → late +0.057 的衰减；③ 2024-02 / 2026-07 小市值拥挤度崩塌窗口的尾部回撤。
  协议沿用"样本内选参 → 纯 OOS 冻结 → WFA"；上一轮候选正是被纯 OOS 否决，size 轮应预期同样结局。
- 教训：**覆盖率不足会直接翻转定性**——8-31 那次 size_neg 在 8% 覆盖下被判"淘汰"，全覆盖后成为第一候选。
  低覆盖存档在 `reports/_prev_lowcov/`；其余 23 个价量因子两次结果几乎不变（rev5 IC 0.08983→0.08986），
  可作链路稳定性对照。回填合并前的原文件备份在 `data/factors/_premerge_bak/`（确认无误后可删）。

## 治理修正收尾（2026-09-02，评估报告强制项 #1）

- **问题**：`lowvol_indz` 在 `live_allowlist` 中但 `data/meta/admission_status.csv` 无 PASS 记录；
  历史绝对收益被当作上市依据。修正（对齐评估"三档能力表"）：
  - `config/settings.yaml`：`live_allowlist: []`（空 = 无人准入，而非放行），新增
    `paper_allowlist: [lowvol_indz]`——只许生成 Paper T+1 信号（人工确认/对账），禁止实盘晋级。
  - `quart/pipeline.py` 新增 `signal_tier(cfg, name)` → live/paper/ValueError 纯函数守卫；
    run_daily 对 paper 档注入"【Paper 候选】仅限模拟盘"警告并写入计划 notes。
  - 台账落版本库：`.gitignore` 定点放行 `data/meta/admission_status.csv` 与
    `admission_details/`；首条记录为 `lowvol_indz passed=False failed=excess_cagr_1x_min`
    （2026-09-02 复跑门禁，证据：1x CAGR 7.53%/Sharpe 0.69、WFA OOS ≈1.78%/0.28、等权超额为负）。
  - `api/strategy_api.py`：`live_signal_choices()` = live∪paper；目录三档标注
    （✅准入/📝Paper候选/🔬研究）；补齐 `index_enhancement`/`factor_portfolio` 元数据。
  - 前端：glossary 增加 `paper_allowlist` 行；strategy_monitor 下拉与三档说明引用台账证据链。
- **§12 收官**：RESEARCH-002 行已标 ⚠️（研究闭环但门禁 FAIL，0 个新因子可准入）；
  QA-001 行已标 ✅（15 个故障演练用例）；BROKER-002 行标 ⛔ BLOCKED（live_allowlist 为空 +
  无真实券商凭证/审批，解锁路径已写入表格）。§12 全部 15 包均有终态。
- **验收**：ruff 干净（改动文件）；771 passed（4 个 artifacts PermissionError 为 Windows
  临时目录已知抖动，单跑通过）；mypy 33 errors/10 files 与基线一致。

