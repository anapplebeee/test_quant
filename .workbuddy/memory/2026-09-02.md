# 2026-09-02 工作日志

## 企业微信 / 微信 告警接口接入
- 新增 `quart/notify/wecom.py`：企业微信群机器人 webhook 通知（markdown），支持加签 secret，环境变量 `QUART_WEBCOM_WEBHOOK` / `QUART_WEBCOM_SECRET` 优先于 settings.yaml。
- 新增 `quart/notify/wechat.py`：微信个人推送（PushPlus 服务号，手机收通知），环境变量 `QUART_WECHAT_PUSHPLUS_TOKEN` 优先。
- `quart/config_schema.py` 增加 `notify.wecom_webhook` / `wecom_secret` / `wechat_pushplus_token` 三项。
- `config/settings.yaml` 的 `notify:` 段补充对应三项（默认空）。
- `quart/pipeline.py` 每日信号推送处接入：钉钉 + 企业微信 + 微信三路同步发送，未配置自动跳过。
- 验证：模块导入、config 校验无报错、未配置时安全跳过返回 False。
- 注：凭据一律走环境变量避免入库；用户需自行提供 webhook key / PushPlus token。

## 质量门禁修复与前端缺陷收口（2026-09-02）
- 质量门禁 FAIL 根因与处置：
  1) **18 只伪造 2024 全年序列**（600001/600002/600003/600005/600013/600014/600024/600034/600040~600047/600049/600053，恒定 1e7 成交量 + 随机游走价，含 20 个节假日行触发 QG-042）：物理隔离到 `data/quarantine/` 并并入 `data/meta/quality_blocklist.csv`（QG-030 防回流）。
  2) **23 只 qfq 复权失真**（000408/600039/600066/600188/600809 等，OHLC 负值/NaN 触发 QG-011，952 行）：执行既有 `scripts/repair_qfq_negative.py`，hfq 全量重拉 2019 至今并 replace，全部 all_positive=True；`data/hfq_pins.txt` 落盘 23 只，updater 今后对其恒用 hfq 防复发（此前 pins 文件缺失是复发根因）。
- 复验：全库重跑 evaluate_quality_gate，QG-011 消失；残余 QG-042 仅剩 2026-08-31/09-01 两日（23 只重拉至最新而基准停在 08-28），由主板增量更新（含 000300 基准）补齐。
- 前端 8 项修复：周一~周日中文映射修正；`get_benchmark_label()` 去除基准硬编码；归因页启动 toast 改内联提示；回测下拉补齐 index_enhancement/factor_portfolio；诊断页冗余 get 清理；数据总览改走 `api.data_api.get_quality_gate_status()`（UI-001 最后残留）；策略监控收尾按实例 id 取输出并传 since=started_at；归因/诊断/词典/风险 4 静态页加整页刷新按钮（Gradio 6.26 用 `.click(js=...)`）；风险页构建期 toast 全部改内联。
- 验证：ruff 修改文件全过；进程内 `create_app()` 构建成功（13 Tab）；mypy quart 维持基线 33/10。
