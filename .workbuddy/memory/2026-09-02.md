# 2026-09-02 工作日志

## 企业微信 / 微信 告警接口接入
- 新增 `quart/notify/wecom.py`：企业微信群机器人 webhook 通知（markdown），支持加签 secret，环境变量 `QUART_WEBCOM_WEBHOOK` / `QUART_WEBCOM_SECRET` 优先于 settings.yaml。
- 新增 `quart/notify/wechat.py`：微信个人推送（PushPlus 服务号，手机收通知），环境变量 `QUART_WECHAT_PUSHPLUS_TOKEN` 优先。
- `quart/config_schema.py` 增加 `notify.wecom_webhook` / `wecom_secret` / `wechat_pushplus_token` 三项。
- `config/settings.yaml` 的 `notify:` 段补充对应三项（默认空）。
- `quart/pipeline.py` 每日信号推送处接入：钉钉 + 企业微信 + 微信三路同步发送，未配置自动跳过。
- 验证：模块导入、config 校验无报错、未配置时安全跳过返回 False。
- 注：凭据一律走环境变量避免入库；用户需自行提供 webhook key / PushPlus token。

## 质量门禁修复与前端缺陷收口（2026-09-02）
- 质量门禁 FAIL 根因与处置：
  1) **18 只伪造 2024 全年序列**（600001/600002/600003/600005/600013/600014/600024/600034/600040~600047/600049/600053，恒定 1e7 成交量 + 随机游走价，含 20 个节假日行触发 QG-042）：物理隔离到 `data/quarantine/` 并并入 `data/meta/quality_blocklist.csv`（QG-030 防回流）。
  2) **23 只 qfq 复权失真**（000408/600039/600066/600188/600809 等，OHLC 负值/NaN 触发 QG-011，952 行）：执行既有 `scripts/repair_qfq_negative.py`，hfq 全量重拉 2019 至今并 replace，全部 all_positive=True；`data/hfq_pins.txt` 落盘 23 只，updater 今后对其恒用 hfq 防复发（此前 pins 文件缺失是复发根因）。
- 复验：全库重跑 evaluate_quality_gate，QG-011 消失；残余 QG-042 仅剩 2026-08-31/09-01 两日（23 只重拉至最新而基准停在 08-28），由主板增量更新（含 000300 基准）补齐。
- 前端 8 项修复：周一~周日中文映射修正；`get_benchmark_label()` 去除基准硬编码；归因页启动 toast 改内联提示；回测下拉补齐 index_enhancement/factor_portfolio；诊断页冗余 get 清理；数据总览改走 `api.data_api.get_quality_gate_status()`（UI-001 最后残留）；策略监控收尾按实例 id 取输出并传 since=started_at；归因/诊断/词典/风险 4 静态页加整页刷新按钮（Gradio 6.26 用 `.click(js=...)`）；风险页构建期 toast 全部改内联。
- 验证：ruff 修改文件全过；进程内 `create_app()` 构建成功（13 Tab）；mypy quart 维持基线 33/10。

## 03:30 策略/因子研究核查 + 修复(commit 3 个已 push hqm@github)
- 核查前期"四部分调研报告"的准确性,3 处实质错误:
  1) "dual_ma 权重可超 100%"不成立(weight=min(1/n,max_weight) 恒 ≤1);
  2) "600372 并购退市 legacy 持仓"归因错误——600372=中航机载(原中航电子,
     吸收合并方)从未退市,数据 2019-2026 连续无缺口;真正退市方 002013 本地无数据;
  3) "N1 行业中性动量"方向错——审计 IC=-0.0372/ICIR=-0.27 淘汰,反方向
     行业中性反转 rel_ind_rev20 IC=+0.0372/ICIR=+0.27 才有效。
  另确认:引擎两污染源(卖滑点 1-slip、signal_offset+次日开盘)已正确修复,
  负 alpha 是真实信号问题非测量假象(vol20_neg ICIR=0.014 佐证)。
- 修复(3 commit 已 push):
  ① dual_ma:无金叉→{FLAT:1.0} 清仓 + max_overshoot_pct(0.15)剔除超买 +
     金叉新鲜度升序选股;回测对比(2023-2025)最大回撤 -94.8%→-56.6%,
     年化 -56.8%→-20.8%(7bc24f5);
  ② 退市过滤防线 quart/data/delisted.py + factor_portfolio 价格防御 +
     filter_st 离线化(本地 stock_names.parquet 优先,修网络卡 23min 阻塞;
     security_master.parquet 缺失致退市检查形同虚设)(84aafe7);
  ③ rel_ind_mom20 因子新增(负向证据)+ 研究报告 factor_strategy_research.md(04e367c)。
- 报告文件前版 factor_strategy_research.md/rebench_compare.csv 在当前工作区不存在
  (双基准数字不可复核),已重写完整备忘录至仓库根目录。

## 04:25 引擎检视 + 新因子/新策略(commit 4 个已 push)
- 引擎全面检视发现并修复 2 个真实 bug:
  ① metrics.py bench_excess_cagr 用算术差(cagr策略-cagr基准),基准强势时
     负超额被夸大近一个量级(实测 +20% vs +145% → 算术差 -125% vs 相对净值
     -51%);改相对净值口径,键名兼容(81c8c20);
  ② constructor 冻结持仓超单票上限直接 raise → 600803 停牌日回测崩溃;
     冻结持仓豁免单票上限(保留至解冻),可交易持仓仍受上限(4e7b537)。
- 新因子挖掘(2022起审计):intraday_rev10_neg(10日日内反转)IC+0.073/ICIR+0.45
  候选入选(85acbfd);rel_ind_rev60 观察(反转持续性);close_pos20_neg/
  overnight_rev10 淘汰。
- 新策略:factor_portfolio 新配置(intraday_rev10_neg+rel_ind_rev20+rev5)
  对比旧默认(vol20+amp20+lottery20):Sharpe -0.55→-0.03,对等权超额
  -20.05%→-17.73%,年化 -8.90%→-6.25%;仍负 alpha(ICIR 未过 0.5)。
- 前期报告"600372 停牌/退市 legacy"归因修正:bug 类型(停牌持仓未容错)
  属实,但具体股票是 600803 停牌而非 600372 退市。
