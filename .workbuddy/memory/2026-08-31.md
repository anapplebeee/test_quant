## 2026-08-31 Codex 进度快照落盘并提交 git

- 核实 codex 会话遗留改动（23 修改 + 11 新增路径），定向测试 8 文件 66 passed。
- 新增 CODEX_PROGRESS.md 记录已完成/未完成/验证记录，提交 2e7f1fb（37 files, +2168/-200）。
- 已落地：manual_trading API+前端页、operations 页、broker/paper 模拟盘、交易日历、lowvol_indz 默认策略、resolve_params 优先级修复。
- 遗留：未对账强制阻断审批、节假日测试、执行偏差统计、全量测试+静态检查、券商 CSV 列映射。
- 环境注意：pytest 需用项目 venv `.venv/Scripts/python.exe`，管理版 Python 无 pytest，uv 不在 PATH。

## 2026-08-31 GLM 四阶段执行（codex 规划收尾 + 策略研究 + 前端修复）

- 完成 codex 规划遗留：券商 CSV 列映射 broker_profiles、端到端流水线测试、节假日测试、策略准入状态前端表、文档勾选同步；修复 store 空 glob bug。commit 2650497，274 tests passed。
- 策略研究：本地池仅 195 只（全市场 3215）；实验矩阵 15 组——top50 +1.9pp / 60d +2pp / rev0.3 +0.6pp / 择时 +1.35pp，B2 组合全样本 +11.2%/Sharpe 0.74，但 WFA OOS -0.1%/衰减比 0.62 → 不改默认参数，P0=补全市场数据后复验。报告 reports/strategy_optimization_2026-08-31.md（git add -f 入库）。commit fe234e3。
- 前端修复：风险/监控页旧 per-symbol parquet 路径失效→统一 BarStore（此前 VaR/流动性全坏）；手动交易页操作后级联刷新（.then 依赖链在组件就绪后追加，避免 UnboundLocalError）。commit 57e1789。
- 经验：手动交易 repository 已内置 approve 对账门禁与 execution_summary，写进度文档前要先 grep 验证；前端组件间级联刷新须在所有组件创建后注册 .then。

## 2026-08-31 GLM 第二轮：架构检视 + 市场分析落地策略（commit 1daa81f / 38f6ff8）

- 架构检视：新增 ARCHITECTURE_OVERVIEW.md（分层/时序/页面映射/不变量）。
  P0 修复：data_api/backtest_api 直读旧 per-symbol parquet（分区迁移后股票数=0/成本明细失败）→ BarStore 双布局；
  归因页 np.random 占位/回测诊断假WFA/因子生态假IC → 真实数据；首页红涨绿跌；参数词典动态注入 config。
- 市场分析（2026-08 末）：万得全A/上证 PE 分位 87-97% 高估、沪深300 PB 偏低、上证50 股息率 3.16%、高低切换。
- 策略落地：lowvol_indz → top50/60d/rev0.3（B2 组合），验证 CAGR +5.4%→+11.2%、Sharpe 0.42→0.74（本地195只池）。
  风险：WFA 衰减比 0.62，预期打折；全市场数据补齐后复验。
- 全量测试 274 passed。push 未完成：环境无 GitHub 凭据（无 GCM/无 token），需用户 PAT 或手动 push。

## 2026-08-31 GLM：指数按板块分类 + 多指数拉取（commit c068084）

- 问题：数据总览"指数数量"只有总数，未区分上证/深证/创业板等；本地仅 1 个指数。
- 解决：新增 quart/data/index_catalog.py（9 指数清单+板块分类）、scripts/update_indices.py（批量拉取）、
  data_api.get_index_coverage + index_boards、数据总览页覆盖明细表、操作中心"更新常用指数"按钮。
- 已拉取 9/9 指数至 2026-08-28（index_count 1→170）。全量测试 274 passed。

## 2026-08-31 GLM：完成阶段 F 券商 API 准备（commit 055e132，279 tests）

- quart/broker/sync.py：券商回报统一写入账本（record_fill 同一管线，来源 PAPER_BROKER）。
- paper_trade_action：APPROVED 计划 → PaperBroker 状态机 → 成交回报 → 入账 → 计划状态推进（API 订单状态机）。
- 前端手动交易页加「Paper Broker 模拟执行」联调入口。
- broker_profiles.convert_broker_xlsx（openpyxl）+ io.import_broker_fill_file 自动分流 CSV/XLSX。
- 教训：编辑 api/manual_trading_api.py 时 Edit 的 old_string 误吞 execution_view 函数头，须在提交前 grep 函数完整性；前端 click 注册别重复（曾把 paper_button.click 复制两遍）。

## 2026-08-31 GLM：全量代码审查修复（commit 6bfa5b4，283 tests）

- 2 并行子代理扫描 + 主代理验证，修 11 项真实问题：
  P0：factor_research_ext 财务因子 lag 自然日→交易日（前视偏差）；2 个 diag 脚本直接实例化崩溃→build_strategy
  P1：universe PIT 空列表不回退快照（前视）；artifacts 指纹全市场扫描；4 CLI --save-dir 硬编码；factor_research/train_ml 空保护
  P2：腾讯源异常阻断东财兜底；腾讯源 volume 单位不一致（000=手/其余=股，实测 000001≈99.5/600519≈1.0）→量价关系归一化；config_schema 补 11 键
- 暂缓（口径变更需确认）：ST 历史过滤、等权基准停牌口径、qfq/hfq 边界混仓
- 经验：volume 单位用 amount/(close*volume) 反推很有效；改 api 文件注意 Edit old_string 别吞相邻函数

## 2026-08-31 重启 Gradio 服务 + 修 daily_signal 启动崩溃

- 重启前状态：PID 32068 持 7860 端口，另有 10680 残留；清理后用 .venv 启动。
- **启动崩溃根因**：commit 1daa81f 把 `glob.glob()`(返回 str) 重构为 `Path.glob()`(返回 Path)，
  但 `frontend/pages/daily_signal.py:26` 下游仍做 `f.replace("signal_","").replace(".md","")`
  → `Path.replace(target)` 是**重命名文件**方法，只接受 1 参 → TypeError，create_app() 直接挂。
  修复：`f.stem.replace("signal_", "")`。283 tests passed。
  教训：Path.glob 重构必须同步检查下游字符串处理；该提交声称"274 passed"但页面 render() 路径无测试覆盖。
- 旧日志中 "Value: momentum_rotation_xxx is not in the list of choices: ['000001'...]" 
  **非代码 bug**：home.py:130 result_dd(run_id) 与 data_overview.py:67 stock_selector(top100代码) 
  组件序号相邻，服务端重启后组件 ID 重新分配，浏览器持旧 session → 值送错组件。解法：Ctrl+F5 强刷。
- 环境观察：Gradio 6.26 启动后父进程为 .venv python，**实际持端口的子进程是系统 Python312**
  (PATH 解析)，存在依赖版本不一致隐患，待观察。
- Git Bash 下 `taskkill //PID` 参数会被转义失败，改用 PowerShell `Stop-Process -Id`。

## 2026-08-31 前端框架选型诊断（回答"前端什么 AI 好用"时的架构发现）

- **关键架构事实**：`api/` 2320 行经 grep 验证**零 Gradio 依赖**（research_api.py:173 那处只是注释）。
  frontend/ 2553 行 / 13 页。→ 换前端框架只需重写 frontend/，api/ 可直接包 FastAPI，迁移成本远低于常规。
- **Gradio 选型错配（重要）**：多个独立来源一致指出 Gradio 定位是 ML/LLM demo，**不适合数据 dashboard**——
  "no real grid layout, sparse chart support"、"apps are stateless by default"。
  而 quart 是 13 页回测/风控/归因分析台，属 Dash/Streamlit 地盘。
  → 这解释了今早的组件 ID 错位 bug：不是偶发，是 Gradio 状态模型弱的**结构性症状**。
- 三条路径与工作量：A 留 Gradio（0 行，天花板低）/ B 换 Dash（重写 2553 行，得细粒度回调+AG Grid）
  / C 换 FastAPI+React（重写+加 REST，此时 v0/Lovable 才有用）。建议先 A 后按痛点决定 B。
- **工具类别分水岭**：v0/Lovable/Bolt 全部输出 React/Next.js，对 Python Gradio 项目零用；
  能改本项目的只有编码 Agent（Claude Code/Cursor/Codex）。选错类别是常见误区。
- 已验证 Gradio 6.26 具备 `gr.Server` / `mount_gradio_app` / `BrowserState` →
  存在"保留 Gradio 队列引擎 + 自定义 React 前端"的渐进迁移路径，不必一次性推翻。
- 调研避坑：搜到大量 AI 生成的 SEO 垃圾文（同站 Copilot 报价 $10 vs $19 自相矛盾、
  查无此物的"AI Buddy"），工具选型类信息必须交叉验证多来源。
