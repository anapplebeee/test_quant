# Quart 商业级量化平台改造大纲

> 文档定位：本文件是 Quart 从“日频研究与人工信号平台”升级为“可审计、可验证、可模拟盘、可逐步实盘”的总体改造蓝图。
>
> 风险声明：任何架构或策略都不能保证 A 股市场中的高额稳定收益。本项目应以“严格样本外超额、可控回撤、成本后盈利、容量匹配、实盘偏差可解释”为目标。

配套文档：

- 当前实现与已知偏差：[`ARCHITECTURE_OVERVIEW.md`](ARCHITECTURE_OVERVIEW.md)
- 目标模块、运行拓扑与迁移阶段：[`docs/TARGET_ARCHITECTURE_V3.md`](docs/TARGET_ARCHITECTURE_V3.md)
- 工作包、依赖、PR 门禁与发布流程：[`docs/DEVELOPMENT_COORDINATION.md`](docs/DEVELOPMENT_COORDINATION.md)
- 长期架构决策：[`docs/adr/README.md`](docs/adr/README.md)

当本文的长期方向与目标架构文档发生冲突时，以已接受的 ADR 和目标架构文档为准；本文继续负责里程碑与商业能力差距，不重复定义领域契约。

## 1. 当前定位与总体判断

当前项目已经具备以下基础：

- AKShare 等数据采集与本地 Parquet/DuckDB 存储；
- 统一策略接口、日频回测、T+1 开盘执行；
- 佣金、印花税、过户费、滑点、涨跌停、停牌、整手约束；
- Walk-forward、随机基线、因子研究、ML 排名策略；
- Artifact 运行制品、配置校验、API 和 Gradio 前端；
- 每日信号生成、钉钉推送和人工下单计划。

当前准确定位是：**个人或小团队使用的 A 股日频研究与交易计划平台**。

当前实施边界进一步明确为：**平台在 T 日收盘后生成 T+1 信号和交易计划，用户在券商端手动下单，平台通过人工录入或成交文件导入同步成交、持仓和现金。券商 API、自动报单和订单回报订阅属于后续阶段。**

手动交易阶段的详细落地方案见 `MANUAL_TRADING_T1_SYNC_PLAN.md`。该阶段先建立计划、成交、持仓批次、账户快照和对账领域模型，未来接入券商 API 时只新增 Broker Adapter，不推翻现有账户与交易账本。

截至 2026-08-30，手动交易第一版最小闭环已实现：SQLite 账本、旧持仓迁移、每日 DRAFT 计划、审批/取消、成交回填、T+1 持仓批次和确认式账户对账。前端操作页、权威交易日历、严格未对账阻断和券商 API 仍按后续里程碑推进。

距离商业级系统的主要差距：

1. Point-in-Time 数据链不完整，正式回测仍可能回退到当前成分股；
2. Walk-forward 测试段缺少连续预热、持仓和资金状态；
3. A 股交易规则按代码前缀硬编码，缺少规则版本和证券状态；
4. 风控主要停留在单票权重，缺少组合级风险预算和状态机；
5. 策略直接输出 Top-K 等权，缺少独立组合构建层；
6. 实盘只有委托计划，没有订单、成交、撤单、对账闭环；
7. 模型、特征、标签、预测缺少完整注册和漂移治理；
8. 运行监控、权限、灾备、多账户等生产能力不足。

## 2. 改造原则

### 2.1 正确性优先于功能数量

- 在 PIT、交易规则、成交模拟和 OOS 验证可信前，不以新增策略数量为目标；
- 正式报告不得引用当前股票池回测历史年份的结果；
- 所有收益指标必须同时报告成本、容量、基准、回撤和样本外口径。

### 2.2 研究、组合、风险、执行分层

目标流水线：

```text
Point-in-Time Data
    -> Feature / Factor
    -> Expected Return / Ranking
    -> Portfolio Constructor
    -> Pre-trade Risk
    -> Order Intent
    -> Execution / Broker
    -> Fill / Position / Cash
    -> Attribution / Monitoring
```

策略只负责输出预测或选股分数，不直接承担全部仓位、风险和成交职责。

### 2.3 回测、模拟盘、实盘共享核心领域模型

- 共用证券主数据、交易规则、组合构建、风控、订单模型和费用模型；
- 差异只由数据源和执行适配器体现；
- 禁止回测和实盘各维护一套独立业务逻辑。

### 2.4 所有结论必须可复现

每次研究或运行必须记录：

- 数据快照与数据版本；
- 股票池和规则版本；
- 配置和策略参数；
- 代码提交版本；
- 模型、特征和标签版本；
- 运行时间、环境和随机种子；
- 指标、持仓、订单、成交和异常记录。

## 3. 目标目录结构

建议逐步演进为以下结构，不要求一次性搬迁现有文件：

```text
quart/
├── data/
│   ├── sources/               # 多数据源适配器
│   ├── pit/                   # PIT 成分、行业、财务、证券状态
│   ├── corporate_actions/     # 分红送转配股及复权因子
│   ├── quality/               # 数据质量规则与阻断
│   ├── catalog.py             # 数据集注册与版本
│   └── snapshots.py           # 不可变数据快照
├── market_rules/
│   ├── calendar.py            # 交易日历和交易时段
│   ├── security_master.py     # 证券主数据
│   ├── rule_engine.py         # 按日期和状态解析规则
│   ├── lot_rules.py           # 最小申报数量、数量单位
│   ├── price_limits.py        # 涨跌幅及无涨跌幅情形
│   └── order_price_rules.py   # 价格笼子、最小价位
├── research/
│   ├── factors/               # 因子定义
│   ├── labels/                # 可交易标签定义
│   ├── validation/            # WFA/CPCV/Bootstrap/PBO
│   ├── baselines/             # 随机、等权、行业基线
│   └── experiments/           # 实验编排
├── strategy/
│   ├── alpha_model.py         # 统一输出预测收益/评分
│   ├── momentum.py
│   ├── lowvol_composite.py
│   ├── quality_value.py
│   └── ml_rank.py
├── portfolio/
│   ├── constructor.py         # 组合构建统一接口
│   ├── optimizer.py           # 约束优化
│   ├── covariance.py          # 风险协方差
│   ├── exposure.py            # 行业/风格/Beta 暴露
│   ├── turnover.py            # 换手与偏离阈值
│   └── constraints.py         # 组合约束
├── risk/
│   ├── pre_trade.py           # 交易前风控
│   ├── intraday.py            # 盘中风控
│   ├── post_trade.py          # 交易后风控
│   ├── limits.py              # 风险限额
│   ├── state_machine.py       # NORMAL/REDUCE/HALT
│   └── kill_switch.py
├── execution/
│   ├── orders.py              # Order/Fill/Cancel 领域模型
│   ├── state_machine.py       # 订单生命周期
│   ├── simulator.py           # 回测/模拟成交
│   ├── cost_model.py          # 滑点和冲击成本
│   ├── reconciliation.py      # 账户对账
│   └── brokers/               # MiniQMT 等适配器
├── portfolio_accounting/
│   ├── account.py             # 现金、冻结资金、持仓
│   ├── ledger.py              # 不可变账本
│   └── pnl.py                 # 已实现/未实现损益
├── models/
│   ├── registry.py            # 模型注册表
│   ├── features.py            # 特征版本
│   ├── predictions.py         # 预测版本与保鲜期
│   └── monitoring.py          # 漂移和失效监控
└── observability/
    ├── metrics.py
    ├── logging.py
    ├── alerts.py
    └── audit.py
```

## 4. P0：回测与数据可信度改造

### 4.1 修复 Walk-forward 连续上下文

当前风险：测试段单独切片后重新准备策略，导致因子预热缺失、账户重置和调仓状态丢失。

修改要求：

- 策略声明 `required_history_days`；
- 测试段前加载足够历史上下文，但只统计测试日起收益；
- 测试开始时不得使用测试期未来数据；
- 明确支持“每折独立账户”和“连续 OOS 账户”两种模式；
- 正式报告默认使用连续 OOS 账户；
- 调仓日历、持仓记忆和策略状态可序列化；
- 对重叠测试窗口禁止重复计算同一交易日收益；
- WFA 结果记录每折成交笔数、暴露、换手和成本。

验收标准：

- 60 日策略在测试首日可以基于测试日前历史产生合法信号；
- OOS 曲线每个日期唯一、连续且无重复；
- 固定参数整段回测与等价连续 WFA 在同口径下近似一致；
- 任意 fold 无法读取其测试结束日之后的数据。

### 4.2 正式回测强制 Point-in-Time 股票池

修改要求：

- 增加 `research_mode: exploratory|formal`；
- `formal` 模式缺少 PIT 成分时直接失败；
- 当前快照回退结果必须标记 `NON_PIT`；
- Artifact 清单写入股票池来源、覆盖区间和质量等级；
- 历史股票池按交易日查询，不只在回测开始时静态确定；
- 历史行业分类和证券状态采用同样的 PIT 规则。

验收标准：

- 正式回测无法静默使用当前成分股；
- 任意回测日均能导出当日可投资股票池证据；
- 新上市、退市、调入和调出日期测试齐全。

### 4.3 建立证券主数据和公司行为账本

证券主数据至少包含：

- 交易所、板块、证券类型；
- 上市、退市、暂停上市和恢复上市日期；
- ST、风险警示、退市整理状态及生效区间；
- 证券简称历史；
- 最小申报数量和数量单位；
- 涨跌幅规则类别。

公司行为至少包含：

- 现金分红；
- 送股、转增、拆并股；
- 配股和增发；
- 除权除息日期；
- 原始价格和复权因子版本。

禁止仅依赖会随未来公司行为变化的整段前复权价格复现历史运行。

### 4.4 建立数据质量阻断

在任务进入因子、回测和信号阶段前执行：

- 新鲜度检查；
- 日期重复和缺口；
- OHLC 合法性；
- 价格、成交量和成交额异常；
- 复权跳变；
- 多源一致性；
- 股票池覆盖率；
- 基准覆盖率；
- PIT 数据覆盖率。

数据质量失败应阻断正式回测和实盘信号，而不只是打印警告。

## 5. P0：A 股交易规则引擎

### 5.1 规则必须按日期版本化

规则键至少包含：

```text
exchange + board + security_type + status + effective_date
```

需要覆盖：

- 主板、创业板、科创板、北交所；
- ST/风险警示和退市整理；
- 新股上市前若干日无涨跌幅；
- 不同板块最小申报数量；
- 零股卖出规则；
- 价格最小变动单位；
- 连续竞价价格笼子；
- 集合竞价、连续竞价、收盘集合/盘后定价；
- 临时停牌和全天停牌。

### 5.2 回测规则与订单校验共享

- 回测执行模型通过规则引擎判断订单是否合法；
- 实盘生成订单前使用相同规则校验；
- 券商拒单原因回写规则监控；
- 规则版本进入 Artifact 指纹。

验收标准：

- 科创板、创业板和主板数量规则分别测试；
- 新股无涨跌幅阶段不会被错误拒单；
- 历史规则变更前后使用不同版本；
- 回测与实盘计划对同一订单给出一致合法性结论。

## 6. P0：组合级风控闭环

### 6.1 统一风险限额

取消策略内上限和风险上限相互冲突的设计：

- 策略输出原始分数或建议权重；
- 组合构建器处理软约束；
- 风控层执行硬约束；
- 所有环境读取同一风险限额版本。

### 6.2 风险维度

必须加入：

- 单票权重；
- 单行业权重；
- 市场 Beta；
- 规模、价值、动量、波动等风格暴露；
- 高相关性持仓簇；
- 组合预期波动率；
- 单日和滚动最大回撤；
- 单日换手和交易费用预算；
- 单票成交额参与率；
- 停牌和跌停不可退出风险；
- 数据、模型和账户异常风险。

### 6.3 风控状态机

建议状态：

```text
NORMAL      正常交易
REDUCE      禁止加总风险，只允许降仓
HALT        禁止新增订单
LIQUIDATE   按可成交性逐步退出
RECOVERY    对账和人工确认后恢复
```

`max_daily_loss_pct` 必须接入账户净值更新和状态机，不能只存在配置文件。

验收标准：

- 回测、模拟盘、实盘使用同一风控链；
- 任一风控动作均记录原因、输入和输出；
- 触发 HALT 后不能生成新增风险订单；
- 停牌或跌停导致无法退出时持续保留退出意图。

## 7. P1：独立组合构建层

### 7.1 输入与输出

输入：

- Alpha 预测或排名；
- 当前持仓和现金；
- 协方差或风险模型；
- 行业和风格暴露；
- 流动性与交易成本；
- 风险限额。

输出：

- 目标权重；
- 预期收益、风险和换手；
- 每条约束的使用量；
- 无法满足约束时的明确失败原因。

### 7.2 第一版优化目标

```text
maximize:
    alpha · weight
    - risk_aversion * portfolio_variance
    - turnover_penalty * turnover_cost
```

约束：

- 权重非负且总权重不超过 100%；
- 单票、行业和风格暴露上限；
- 单日换手上限；
- ADV 参与率上限；
- 最低现金比例；
- 不可交易持仓冻结。

### 7.3 降低不必要交易

- 排名缓冲；
- 目标权重偏离阈值；
- 最小订单金额；
- 成本收益比阈值；
- 对现有持仓施加延续奖励；
- 优先卖出失效最强、流动性最好的持仓。

## 8. P1：策略调整

### 8.1 动量策略

从单一 60 日动量调整为多周期、行业中性的复合信号：

- 1、3、6、12 个月动量；
- 剔除或反向使用最近 5 至 20 日短期反转；
- 波动率缩放；
- 行业内标准化；
- 涨停不可买、连续涨停和拥挤度过滤；
- 财务质量和异常公司过滤；
- 用风险预算替代 MA20 全仓/空仓二元切换。

不应直接将未经 OOS 验证的固定权重写入生产配置。

### 8.2 低波策略

- 识别低波、低振幅和彩票因子的高度相关性；
- 加入质量、估值和中期动量，避免单一防御风格；
- 使用 PIT 行业分类进行行业中性；
- 将策略状态和持仓记忆持久化；
- 调仓频率由成本和信号衰减共同决定；
- 将低波定位为多策略组合中的防御模块。

### 8.3 质量价值策略

新增基础策略，但必须严格使用公告日数据：

- 盈利质量；
- 现金流质量；
- 杠杆和偿债能力；
- 资产增长和应计项目；
- 估值；
- 行业内比较。

财务标签必须按市场实际可获得时间对齐，禁止按报告期结束日直接使用。

### 8.4 ML 排名策略

- 训练、验证、测试按时间切分；
- 标签使用扣除可实现成本后的未来收益；
- 训练特征全部符合 PIT；
- 模型分数不允许无界前向填充；
- 模型注册表记录数据、特征、标签和代码版本；
- 监控 IC、RankIC、覆盖率、换手、收益衰减和行业漂移；
- 设置自动降权和停用条件；
- 线性模型作为长期基准，复杂模型必须证明增量价值。

### 8.5 多策略资金分配

在单策略验证通过后，再构建策略组合：

- 动量；
- 质量价值；
- 低波防御；
- ML 增强；
- 现金或低风险仓位。

分配方法优先考虑风险贡献、相关性和回撤，而不是按历史收益最高者分配。

## 9. P1：回测成交与成本模型

当前“次日开盘价 + 固定滑点”只适合粗粒度日频研究。

需要补充：

- 集合竞价成交概率；
- 开盘涨跌停封单和排队；
- 部分成交；
- 按 ADV 参与率计算冲击成本；
- 买卖价差；
- 大额订单分拆；
- 撤单和跨时段重挂；
- 停牌、临停、价格笼子；
- 实际成交数据反向校准成本参数。

成本压力测试至少包含：

- 默认成本；
- 滑点 1.5 倍；
- 滑点 2 倍；
- 不同账户规模；
- 不同 ADV 参与率；
- 开盘、VWAP 和收盘执行假设。

正式策略必须在不利成本场景下仍有合理的正期望。

## 10. P1：研究验证体系

每个候选策略必须经过：

1. 数据和 PIT 检查；
2. 因子方向与经济逻辑检查；
3. 横截面 IC、RankIC 和分组收益；
4. 行业、规模、Beta 和流动性归因；
5. Walk-forward 连续 OOS；
6. Bootstrap 置信区间；
7. 参数稳定性和局部敏感性；
8. 多重检验修正；
9. 成本与容量压力测试；
10. 市场状态分段；
11. 随机、等权、指数和简单模型基线；
12. 模拟盘验证。

建议增加指标：

- Sortino；
- Omega；
- Ulcer Index；
- 信息比率；
- 跟踪误差；
- 上下行捕获；
- 平均和最长回撤恢复期；
- 换手率和单位换手收益；
- Alpha 衰减；
- 容量；
- Deflated Sharpe Ratio；
- Probability of Backtest Overfitting。

## 11. P1：实盘订单与账户闭环

### 11.1 领域模型

至少定义：

- `OrderIntent`：组合层希望交易什么；
- `BrokerOrder`：发送给券商的订单；
- `OrderEvent`：确认、拒绝、部分成交、成交、撤单；
- `Fill`：成交记录；
- `Position`：持仓和可用数量；
- `CashBalance`：现金、可用和冻结；
- `AccountSnapshot`：账户快照；
- `ReconciliationResult`：对账结果。

### 11.2 订单状态机

```text
CREATED
  -> VALIDATED
  -> SUBMITTED
  -> ACKNOWLEDGED
  -> PARTIALLY_FILLED
  -> FILLED
  -> CANCELED / REJECTED / EXPIRED
```

要求：

- 客户端订单号幂等；
- 重启后恢复未完成订单；
- 部分成交后重新计算剩余风险；
- 券商持仓和本地账本定期对账；
- 对账失败进入 HALT；
- 人工操作也必须同步到账本。

### 11.3 上线顺序

```text
历史回测
-> 回放仿真
-> 实时纸面交易
-> 券商模拟盘
-> 小资金实盘
-> 分阶段扩大资金
```

禁止从历史回测直接进入自动实盘。

## 12. P2：生产化能力

### 12.1 可观测性

- 数据更新时间和覆盖率；
- 任务成功率和延迟；
- 信号数量和分布；
- 组合风险暴露；
- 订单拒绝率、成交率和滑点；
- 账户净值和回撤；
- 模型和因子漂移；
- 券商连接和对账状态。

### 12.2 权限与安全

- 数据、研究、审批、交易和管理员角色分离；
- 密钥只通过安全存储和环境注入；
- 实盘订单需要明确环境标识；
- 高风险动作需要审批；
- 全量不可篡改审计日志；
- API 输入、文件路径和任务参数继续执行白名单校验。

### 12.3 可用性与灾备

- 任务幂等；
- 可恢复检查点；
- 数据和制品备份；
- 券商断线重连；
- 主进程崩溃后恢复订单和账户状态；
- 容灾演练和紧急人工流程。

### 12.4 存储演进原则

当前日频规模继续使用 Parquet + DuckDB 即可。只有满足以下条件后再考虑 ClickHouse 或其他分布式存储：

- 分钟/Tick 数据规模显著增长；
- 多用户并发分析成为瓶颈；
- 单机查询和写入无法满足 SLA；
- 已有明确容量测试证据。

## 13. 配置体系调整

建议将配置拆分为：

```text
config/
├── base.yaml
├── research.yaml
├── paper.yaml
├── live.yaml
├── risk_limits.yaml
├── market_rules.yaml
└── strategies/
    ├── momentum.yaml
    ├── lowvol.yaml
    └── ml_rank.yaml
```

规则：

- 环境配置显式区分研究、模拟盘和实盘；
- 实盘不得继承研究环境中的宽松默认值；
- 风险限额单独版本化；
- 参数来源和覆盖优先级可查询；
- 未识别配置字段应直接失败；
- 正式运行保存解析后的完整配置，而不只保存输入片段。

## 14. 测试体系调整

### 14.1 单元测试

- 每种证券和日期的交易规则；
- 组合优化约束；
- 风控状态转换；
- 订单状态转换；
- 费用和冲击成本；
- 公司行为和复权；
- PIT 查询；
- 策略状态序列化。

### 14.2 属性与不变量测试

- 现金、持仓和成交账本守恒；
- 不允许卖出超过可用持仓；
- 不允许买入超过可用现金；
- 订单数量和价格必须符合规则；
- 风控后风险不得高于风控前；
- Artifact 指纹相同应产生相同结果。

### 14.3 集成测试

- 数据更新到每日信号；
- 信号到模拟成交；
- 成交到账户和归因；
- 断线重连与订单恢复；
- 本地账本与券商快照对账；
- 回测与实时纸面交易在相同输入下保持决策一致。

## 15. 实施里程碑

### M1：研究可信度基线

- [x] 修复 WFA 连续上下文和预热（`required_history_days`、PIT 上下文、连续/独立账户、重叠 OOS 去重）；
- [x] 正式回测强制 PIT 股票池（`--research-mode formal` 缺失/覆盖不完整即阻断；exploratory 明确标记 `NON_PIT`）；
- [ ] 增加数据质量阻断；
- [x] Artifact 记录股票池和规则版本（模式、指数、来源、质量等级、`ashare_v1`）；
- [ ] 建立成本与容量压力测试；
- [ ] 重跑全部 README 历史结论。

交付条件：所有对外展示收益均为可复现的连续 OOS 结果。

### M2：规则和组合风险

- [ ] 建立证券主数据；
- [ ] 建立版本化交易规则引擎；
- [ ] 强制统一风险链；
- [ ] 实现组合暴露与风险状态机；
- [ ] 接入 `max_daily_loss_pct`；
- [ ] 增加 Kill Switch。

交付条件：回测、模拟和信号对同一订单使用相同交易与风险规则。

### M3：组合构建和策略升级

- [ ] 实现 Portfolio Constructor；
- [ ] 动量升级为多周期复合；
- [ ] 低波加入质量/价值和行业约束；
- [ ] 建立质量价值策略；
- [ ] 模型注册和漂移监控；
- [ ] 多策略风险预算。

交付条件：策略输出与最终权重分离，换手和暴露可控。

### M4：模拟交易闭环

- [x] 完成手动交易 T+1 同步最小闭环；
- [x] 支持人工录入和 CSV 导入成交；
- [x] 支持 T+1 可卖数量和持仓批次；
- [x] 支持账户快照和差异对账；
- [x] 交易计划具有审批、部分成交和完成状态；
- [ ] 增加计划自动过期和前端操作页；
- [ ] 订单和成交领域模型；
- [ ] 订单状态机；
- [ ] 模拟 Broker Adapter；
- [ ] 自动账户账本；
- [ ] 自动对账；
- [ ] 执行偏差归因。

交付条件：用户仍在券商端手动下单，但连续运行期间无需直接编辑 `holdings.json`；平台能够从人工成交和账户快照重建当前账户状态。

### M5：小资金实盘准备

- [ ] 接入目标券商；
- [ ] 权限、审批和密钥治理；
- [ ] 运行监控与告警；
- [ ] 故障恢复和容灾演练；
- [ ] 数月模拟盘验收；
- [ ] 小资金和低 ADV 参与率上线。

交付条件：订单、成交、持仓、现金、风控和审计形成完整闭环。

## 16. 策略准入标准

候选策略进入模拟盘前必须满足：

- PIT 数据覆盖完整；
- 连续 OOS 收益为正；
- 相对合理基准有稳定超额；
- 参数邻域表现平滑；
- 收益不集中于少数年份或股票；
- 成本加倍后不出现结构性失效；
- 行业、风格和流动性暴露可解释；
- 最大回撤和恢复期符合账户承受能力；
- 有足够交易容量；
- 多重检验和过拟合风险可接受。

进入小资金实盘前还必须满足：

- 模拟盘连续稳定运行；
- 实际成交率和滑点在压力模型范围内；
- 订单和账户对账无未解决差异；
- 风控和 Kill Switch 演练通过；
- 模型、数据和任务告警有效；
- 人工应急流程明确。

## 17. 暂不优先事项

在 P0/P1 完成前暂不优先：

- 大规模增加技术指标；
- 使用深度学习替代基础模型；
- 迁移到复杂分布式数据库；
- 开发更多前端页面；
- 追逐单次参数扫描的最高 CAGR；
- 直接接入自动实盘并扩大资金。

这些工作可能增加功能和展示效果，但不会优先提升收益结论可信度或实盘安全性。

## 18. 建议第一批代码任务

建议按以下顺序进入实际开发：

1. 为 `BaseStrategy` 增加 `required_history_days` 和状态序列化接口；
2. 重构 `run_walk_forward`，支持连续上下文和唯一 OOS 日期；
3. 给正式回测增加 `require_pit_universe=True`；
4. 新增 `SecurityMaster` 和最小可用 `MarketRuleEngine`；
5. 将整手和涨跌幅判断迁移到规则引擎；
6. 将风险链设为回测和信号生成的必选依赖；
7. 实现组合行业暴露、ADV 和换手限制；
8. 新增最小版本 `PortfolioConstructor`；
9. 重跑并废止不符合新口径的旧结论；
10. 在以上稳定后开始订单状态机和模拟 Broker。

## 19. 最终目标

本项目升级完成后的核心能力不是“回测中获得最高收益”，而是能够可靠回答：

1. 某日为什么持有这些股票？
2. 当时使用的数据是否已经公开且可获得？
3. 权重如何由 Alpha、风险、成本和当前持仓共同决定？
4. 订单为什么成交、部分成交、被拒绝或没有成交？
5. 收益来自选股、行业、市场 Beta、择时还是偶然暴露？
6. 实盘与回测差异来自数据、信号、组合、风控还是执行？
7. 当数据、模型、市场或券商异常时，系统如何安全停止并恢复？

只有这些问题都有可审计答案后，才具备讨论长期、稳定、可扩展风险调整收益的基础。
