# Quart 前后端整合与策略升级开发规划

> 创建日期：2026-08-31  
> 本轮边界：完成不依赖券商授权的研究、信号、手动交易与模拟执行闭环。真实券商 API、密钥托管和生产报单必须在明确券商、账户权限及联调环境后实施。

## 1. 开发目标

1. 将日常需要执行脚本的操作迁移到 Gradio 前端，后端统一走参数白名单、任务队列和审计制品；
2. 建立“账户快照 → T 日信号 → T+1 计划审批 → 手工成交回填 → 收盘对账 → 执行复盘”的可视化闭环；
3. 消除前端旧 `holdings.json` 与 SQLite 交易账本并存造成的账户口径漂移；
4. 修复策略配置优先级和默认策略选择，禁止已证伪策略继续作为默认实盘信号；
5. 以成本后、样本外、可解释和可审计为策略准入条件，不承诺固定或高额收益。

## 2. 本轮交付顺序

### P0：前后端账户与交易闭环

- [x] 新增 `api/manual_trading_api.py`，隔离 Gradio 与 SQLite 领域仓库；
- [x] 新增手动交易前端页：账户、计划、审批、成交、对账、复盘；
- [x] 风险管理与策略监控统一读取 SQLite 账户状态；
- [x] 支持计划调减、取消、按交易日过期和人工下单 CSV 导出；
- [x] 支持成交 CSV 自动匹配计划订单和执行偏差统计；
- [x] 未完成收盘对账时阻断新的正式计划审批（`approve_plan` 信号日对账门禁 + 回归测试）。

### P0：命令操作前端化

- [x] 扩展任务白名单：数据刷新、质量扫描、股票池历史、回测、扫描、WFA、信号与存储迁移；
- [x] 前端提供参数化表单，不允许用户输入任意 shell 命令（操作中心页）；
- [x] 所有长任务统一展示排队状态、日志、取消按钮和产物位置；
- [x] 破坏性或写盘操作使用明确确认开关（存储迁移默认仅预演 + 二次确认）。

### P1：策略与风险模型升级

- [x] 默认策略从已证伪的动量策略切换到低频行业中性低波策略；
- [x] 修复"全局参数覆盖策略专属参数"的配置优先级问题（显式 params > overrides > 全局）；
- [ ] 低波策略加入稳健截尾、多窗口风险和趋势质量过滤；
- [x] 默认使用低换手、排名缓冲和更分散持仓（top30 / 45d / buffer 0.5）；
- [x] 前端明确区分"研究/观察/候选/准入"策略状态（策略监控页准入状态表 + live_allowlist 对齐测试）；
- [ ] 策略变更必须通过回测、成本压力和 Walk-Forward 验证后才能升级为正式信号（本轮已完成一次全市场人工门禁，准入自动化校验待补）。

### P1：文档与验证

- [x] 重构 README 为"安装 → 前端启动 → 首次初始化 → 每日操作 → 研究验证 → 故障排查"；
- [x] 同步更新手动交易规划和商业化改造路线图；
- [x] 增加 API、页面构建、T+1、计划状态和策略参数回归测试（含端到端流水线、券商 CSV 映射、节假日日历）；
- [x] 运行相关测试、全量测试和静态检查，记录任何非本轮遗留失败。

## 3. 前后端职责边界

| 层级 | 职责 | 禁止事项 |
|---|---|---|
| `frontend/` | 表单、状态展示、确认交互 | 直接读写 SQLite、拼 shell 命令 |
| `api/` | 输入校验、DataFrame/Markdown 展示模型、领域服务编排 | 放置策略计算和账本 SQL |
| `quart/` | 策略、风险、执行、账户与交易领域逻辑 | 依赖 Gradio |
| `scripts/` | 可自动化 CLI 入口，与前端调用同一领域代码 | 成为唯一可用操作入口 |
| `artifacts/` | 参数、数据版本、指标和文件审计 | 保存密钥或券商凭据 |

## 4. 策略问题判断

### 4.1 当前主要问题

- `momentum_rotation` 在项目现有全市场样本中成本前后均失效，却仍是默认策略；
- 每日信号和部分研究脚本把全局参数作为显式参数传入，可能覆盖 `strategy.overrides`；
- 低波有效性主要来自防御属性，收益对调仓频率和交易成本高度敏感；
- 只看全样本 CAGR 容易把市场 Beta、随机选股和参数搜索噪声误判为 Alpha；
- 当前组合构建仍以等权为主，行业、容量、换手和风险预算约束尚不完整。

### 4.2 本轮调整原则

- 默认信号采用 `lowvol_indz`，但状态仍标记为“候选”，不表述为稳定盈利系统；
- 默认参数采用项目已验证的低频、分散和缓冲方案；
- 动量、双均线和 ML 在未通过新的样本外准入前仅作为研究策略；
- 任何参数优化都必须报告样本外衰减、年度稳定性、成本压力和最大回撤；
- 实盘仓位应从小资金开始，并以券商快照和风险限额为最终约束。

## 5. 完成定义

本轮只有在以下条件同时满足时才算完成：

1. 用户不打开终端即可完成日常手动 T+1 操作；
2. 前端账户、风险页和信号计划使用同一 SQLite 状态；
3. 脚本入口仍可用于自动化，但不再是日常操作的唯一入口；
4. 默认策略不再指向已证伪策略，配置优先级有测试保护；
5. README 能让新用户按顺序完成首次启动和一个完整交易日流程；
6. 规划文件中的完成项与实际代码、测试一一对应。
